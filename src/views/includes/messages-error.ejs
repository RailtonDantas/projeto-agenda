<% if(errors.length > 0) { %>
    <div id="errors" class="bg-red-500 text-black border border-black px-2 py-2 mb-4">
        <% errors.forEach((erro) => {%>
            <%= erro %><br>
        <%})%>
    </div>
<% } %>